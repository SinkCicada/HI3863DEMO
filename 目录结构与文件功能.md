# 目录结构与目录作用

• 这个工程可以分成两层看：第一层是工程根目录 D:\hi3861_hdu_iot_application\hi3861_hdu_iot_application，主要放 IDE 配置、文档和真正的源码根目录 src；第二层是 src，这里才是 OpenHarmony LiteOS for Hi3861 的完整源
  码树、板级适配、第三方库和构建输出。你后面改代码时，绝大多数时间其实只会在 src 下面活动。

##   根目录

  - .deveco：DevEco Device Tool 的工程配置目录。这里的 D:/hi3861_hdu_iot_application/hi3861_hdu_iot_application.deveco/deveco.ini:15 决定了当前环境是 hi3861、使用哪个产品 wifiiot_hispark_pegasus@hisilicon、串
    口号、工具链路径、烧录方式等。你这次编译、烧录、调试时，很多参数都从这里来。
  - .vscode：VS Code/DevEco 的任务和调试配置。比如编译任务、烧录任务、J-Link 调试入口都在这里定义。
  - build：这个根目录下的 build 不是 OpenHarmony 主构建系统源码目录，当前项目里基本没内容，可以理解成工程附带的小工具/占位目录。
  - doc：项目文档、实验指导手册、使用说明。
  - sample：工程附带的顶层样例目录，这份工程里基本不是主编译入口。
  - src：最核心的源码根目录。OpenHarmony LiteOS、Hi3861 板级支持、应用入口、第三方库、构建系统都在这里。
  - README.md、更新日志.txt：工程说明和版本记录。

##   src 总体结构

  - **applications**：应用层入口代码。当前这份工程真正挂到系统里的业务入口在 src/applications/sample/wifi-iot/app/BUILD.gn:16，它把应用组件 app 指向了 //vendor/pzkj/pz_hi3861/demo:demo。
  - **base**：OpenHarmony 的基础能力组件。这里包括日志、启动、外设抽象、安全、升级等通用模块。
  - **build**：OpenHarmony 的构建系统源码，不是你的业务代码。里面有 hb、GN/Ninja 相关脚本、组件定义、产品配置模板、工具链规则等。你这次修复的 src/build/lite/hb/... 就在这里。
  - **device**：芯片/开发板级适配层。当前是 hisilicon/hispark_pegasus，里面放 Hi3861 对应的适配代码、LiteOS SDK、板级 HAL、底层构建脚本。
  - **domains**：领域能力模块。当前能看到 iot，一般放物联网领域组件和聚合逻辑。
  - **foundation**：系统基础框架能力，如通信、分布式调度、应用执行框架等。
  - **out**：构建输出目录。编译后产物、对象文件、target.elf、map、bin 等都在这里。它不属于源码。
  - **prebuilts**：预编译工具和系统资源，比如 build-tools、lite/sysroot 等。GN、Ninja、部分预置工具会从这里取。
  - **productdefine**：产品定义相关内容。它决定某个产品组合使用哪些组件、能力、板型配置。
  - **test**：测试体系，主要是 xdevice、xts 等自动化测试框架。
  - **third_party**：第三方库，比如 mbedtls、paho.mqtt.embedded-c、cJSON、ssd1306、httpd 等。
  - **utils**：基础工具库和通用能力，当前主要看到 native。
  - **vendor**：厂商、板卡供应商、自定义 demo 和 BSP 集中区。你自己的实验代码主要就在这里。

###   base 目录作用

  src/base 下是系统基础组件，不建议随便改，除非你明确在修平台问题。

  - global：全局基础能力。
  - hiviewdfx：日志和诊断相关，像 hilog_lite、hiview_lite 都在这。
  - iot_hardware：IoT 外设能力接口。
  - powermgr：电源管理。
  - security：安全能力，比如 huks、deviceauth。
  - sensors：传感器抽象能力。
  - startup：启动流程和系统参数初始化。
  - update：OTA/升级相关。

###   foundation 目录作用

  这是系统框架层，不是你当前示例逻辑的主要修改点。

  - appexecfwk：应用执行框架。
  - communication：通信栈，例如 Wi-Fi 相关框架、SoftBus Lite 等。
  - distributeddatamgr：分布式数据管理。
  - distributedschedule：分布式调度和服务管理，比如 samgr_lite。

###   build 目录作用

  这是整套工程的构建内核。

  - lite：LiteOS/OpenHarmony Lite 的核心构建目录。hb 命令、组件定义、产品构建逻辑、clean/build 过程基本都在这。
  - common、core、toolchain、templates、config：提供构建规则、模板、默认配置和工具链封装。
  - 你这次报错涉及的几个关键文件都在这里：
      - src/build/lite/hb/common/utils.py:1
      - src/build/lite/hb/common/config.py:1
      - src/build/lite/hb/build/build_process.py:1

###   device 目录作用

  这里是“芯片和板卡真正怎么跑起来”的地方。当前你最关心的是：

  - src/device/hisilicon/hispark_pegasus/hi3861_adapter：Hi3861 对接 OpenHarmony 的适配层，含 HAL、通信、POSIX 兼容等。
  - src/device/hisilicon/hispark_pegasus/sdk_liteos：Hi3861 SDK 与 LiteOS 的底层整合和 SCons 构建部分。很多最终链接、底层驱动、boot 相关脚本在这里。
  - src/device/hisilicon/hispark_pegasus/figures：一些板卡相关图示或资源。

  如果你碰到烧录、底层驱动、Wi-Fi HAL、外设底层兼容问题，通常要往这里看。

###   vendor 目录作用

  这是这份工程最“像课程工程”的部分，也是你最可能改业务代码的地方。当前有多家厂商/教学方目录：

  - bearpi
  - hihope
  - hisilicon
  - hqyj
  - pzkj

  你这份工程当前实际启用的是 pzkj 分支，也就是：

  - src/vendor/pzkj/pz_hi3861

  它下面主要有两块：

  - common：公共 BSP 和通用驱动封装。比如 LED、ADC、UART、PWM 这些板级支持代码。
  - demo：一个个实验/示例工程。你这里能看到 00_template 到 42_ps2 很多目录，每个目录基本就是一个独立实验题。

  demo 目录的组织方式很清晰：

  - 每个子目录一个实验，例如 08_led、17_uart、26_wifi_mqtt、42_ps2
  - 每个实验通常有自己的 BUILD.gn 和一个或多个 *.c
  - 公共底层能力则复用 common/bsp/src/*.c

  当前真正参与编译的是：

  - src/vendor/pzkj/pz_hi3861/demo/BUILD.gn:16
  - 它的 features 里启用了 "42_ps2:template"，其余 demo 大多被注释掉了
  - 所以当前业务代码入口就是 src/vendor/pzkj/pz_hi3861/demo/42_ps2

###   applications 目录作用

  这个目录可以理解成“系统应用挂载点”。虽然你真正的实验代码在 vendor/pzkj/...，但系统不是直接从那儿启动，而是通过 applications/sample/wifi-iot/app 这个入口把 vendor demo 接进来。

  - src/applications/sample/wifi-iot/app/BUILD.gn:16 定义了一个 lite_component("app")
  - 它的 features 指向 //vendor/pzkj/pz_hi3861/demo:demo
  - 再由 vendor/pzkj/pz_hi3861/demo/BUILD.gn 选中某个具体 demo
  - 目前最终落到 42_ps2:template

  所以当前编译链路是：

  1. .deveco/deveco.ini 选择产品 wifiiot_hispark_pegasus@hisilicon
  2. 产品把应用入口挂到 applications/sample/wifi-iot/app
  3. app/BUILD.gn 引入 vendor/pzkj/pz_hi3861/demo:demo
  4. vendor/pzkj/pz_hi3861/demo/BUILD.gn 选中 42_ps2:template
  5. 42_ps2/BUILD.gn 再把 template.c 和若干 BSP 源文件一起编进去

  42_ps2 和 common/bsp 的关系
  你当前这个示例目录：

  - src/vendor/pzkj/pz_hi3861/demo/42_ps2

  里面通常只有：

  - template.c：这个实验本身的主逻辑
  - BUILD.gn：指定当前实验要编哪些源文件
  - 接线图.png：硬件接线参考

  而 src/vendor/pzkj/pz_hi3861/demo/42_ps2/BUILD.gn:14 还会把这些公共 BSP 拉进来：

  - //vendor/pzkj/pz_hi3861/common/bsp/src/bsp_led.c
  - //vendor/pzkj/pz_hi3861/common/bsp/src/bsp_adc.c
  - 以及其他被注释/启用的 BSP 文件

  这意味着：

  - 你要改“实验逻辑”，主要改 42_ps2/template.c
  - 你要改“某个通用驱动实现”，改 common/bsp/src/*.c
  - 你要改“当前实验引入哪些模块”，改 42_ps2/BUILD.gn

###   third_party 目录作用

  这里放第三方依赖库，不是本工程业务入口，但很多能力会间接用到。

  - cJSON：JSON 解析
  - mbedtls：TLS/加密
  - paho.mqtt.embedded-c：MQTT
  - ssd1306：OLED 驱动
  - httpd、sntp 等网络组件

  如果你后面做云平台、MQTT、HTTPS、OLED 显示，这些目录会逐渐有用。

  prebuilts 目录作用
  这是预编译工具和基础资源目录。

  - build-tools：GN、Ninja 这类构建工具
  - lite：LiteOS 相关预置资源和 sysroot

  当构建系统找 gn.exe、ninja.exe、sysroot、部分预编译资源时，通常会从这里取。

###   productdefine 目录作用

  这个目录是产品定义层。它决定：

  - 当前产品名称
  - 板级/芯片组合
  - 应用组件集合
  - 系统能力裁剪

  你现在 deveco.ini 中的 board_frameworks.hb.build.product = wifiiot_hispark_pegasus@hisilicon，本质上就是让构建系统去找对应的产品定义，再把系统组件和应用入口组织起来。

  out 目录作用
  这是构建输出目录，不是源码。

  - 当前在 src/out/hispark_pegasus/wifiiot_hispark_pegasus
  - 包含 obj/、libs/、build.ninja、target.elf、map、bin 等
  - 你报错里很多路径都指向这里
  - 一旦需要“彻底重新编译”，通常清的是这里

###   test 目录作用

  主要是测试框架，不是日常业务开发主战场。

  - xdevice
  - xts

  除非你做自动化测试、兼容性测试，否则平时基本不用动。

  如果你现在只想知道“平时最该看哪些目录”
  按优先级就是这几个：

  1. src/vendor/pzkj/pz_hi3861/demo/42_ps2
     当前实际业务示例代码。
  2. src/vendor/pzkj/pz_hi3861/common
     课程工程公共 BSP 和驱动封装。
  3. src/applications/sample/wifi-iot/app
     系统应用入口，决定当前挂哪个 vendor demo。
  4. src/device/hisilicon/hispark_pegasus
     板级适配、SDK、底层驱动。
  5. src/build/lite/hb
     构建系统，只有构建报错或想改编译流程时才重点看。



# 新增自定义程序需要修改的文件说明

 通常只需要改 3 类文件：**业务代码文件**、
  当前示例的 **BUILD.gn**、**以及串口/工具链配置文件**。

  先说最实用的做法，也就是基于当前已经能编译的 42_ps2 继续改。你至少要改：

  - src/vendor/pzkj/pz_hi3861/demo/42_ps2/template.c
    这是当前真正参与编译的业务主代码。你自己的逻辑一般直接写这里。
  - src/vendor/pzkj/pz_hi3861/demo/42_ps2/BUILD.gn:14
    如果你新增了 c 文件，或者要启用 uart/pwm/i2c/oled 之类的 BSP 源文件，就要改这里的 sources 和 include_dirs。
  - D:/hi3861_hdu_iot_application/hi3861_hdu_iot_application.deveco/deveco.ini:15
    主要确认 upload_port、monitor_port、compiler_bin_path 是否正确。编译不过或烧录不上时，这个文件往往比代码本身更先出问题。

  如果你只是把 42_ps2 改成自己的工程，这通常已经够了。因为当前构建链路已经固定好了：

  - src/applications/sample/wifi-iot/app/BUILD.gn:16 把应用入口指到了 //vendor/pzkj/pz_hi3861/demo:demo
  - src/vendor/pzkj/pz_hi3861/demo/BUILD.gn:16 目前启用的是 42_ps2:template
  - 所以你只要继续沿用 42_ps2，不必再改更上层入口文件

  如果你想“新建一个独立工程目录”，比如新建 src/vendor/pzkj/pz_hi3861/demo/43_my_app，那需要再多改两处：

  1. 新建你自己的目录和文件：
      - src/vendor/pzkj/pz_hi3861/demo/43_my_app/template.c
      - src/vendor/pzkj/pz_hi3861/demo/43_my_app/BUILD.gn
  2. 修改 src/vendor/pzkj/pz_hi3861/demo/BUILD.gn:16
      - 注释掉当前的 "42_ps2:template"
      - 改成 "43_my_app:template" 或你自己定义的目标名

  这种情况下，applications/sample/wifi-iot/app/BUILD.gn 通常不用改，因为它还是统一挂到 vendor/pzkj/pz_hi3861/demo:demo，只是 demo 里面具体选哪个子工程变了。

  如果你还用到了新的硬件驱动或公共封装，才需要再改 common 目录下的文件：

  - src/vendor/pzkj/pz_hi3861/common/bsp/src
  - src/vendor/pzkj/pz_hi3861/common/bsp/include

  例如：

  - 你只写 LED 闪烁逻辑，通常只改 template.c
  - 你要额外用 UART/ADC/PWM，就要在你的 BUILD.gn 里把对应 bsp_uart.c、bsp_adc.c、bsp_pwm.c 加进去
  - 你要自己封装一个新的传感器驱动，才需要新建或修改 common/bsp/src/*.c

  可以把“必须改”和“可能要改”简单记成下面这样。

  必须改：

  - 业务代码：src/vendor/pzkj/pz_hi3861/demo/<你的工程>/template.c
  - 当前工程的构建文件：src/vendor/pzkj/pz_hi3861/demo/<你的工程>/BUILD.gn
  - 串口和工具链配置：.deveco/deveco.ini

  新建独立工程时再改：

  - src/vendor/pzkj/pz_hi3861/demo/BUILD.gn

  通常不用改：

  - src/applications/sample/wifi-iot/app/BUILD.gn
  - src/device/...
  - src/build/...
  - src/foundation/...
  - src/base/...

  除非你遇到的是平台层、驱动层、构建系统层的问题，否则不要先动这些系统目录。

  如果你想要最稳妥的开发方式，我建议你直接复制一份 42_ps2 目录改名成自己的工程，然后只改这 4 个地方：

  1. 新目录里的 template.c
  2. 新目录里的 BUILD.gn
  3. src/vendor/pzkj/pz_hi3861/demo/BUILD.gn
  4. .deveco/deveco.ini 里的串口号

 

# 其他

• 如果你要“新建一套工程，并且能用 GPIO，顺带考虑蓝牙/共存能力”，建议按这套仓库现有的 vendor demo 模式来做。先给结论：

  - GPIO 可以直接做，最稳的入口是 iot_gpio.h。
  - 如果你要更底层地控制引脚复用，再加 hi_io.h 和 hi_gpio.h。
  - 这份源码树里我没有找到可直接用于 BLE/GATT/蓝牙扫描广播的完整公开栈头文件和实现，只有 hi_btcoex_api.h 这种 Wi-Fi/BT 共存控制接口，所以“真正蓝牙业务”在这
    份工程里不能按普通应用那样直接开发。

  1. 你要引用哪些文件

| 功能                                       | 应该引用的头文件                                             | 对应 .c / 实现位置                                           | 说明                                            |
| ------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ----------------------------------------------- |
| GPIO，推荐应用层写法                       | src/base/iot_hardware/peripheral/interfaces/kits/iot_gpio.h:1 | src/device/hisilicon/hispark_pegasus/hi3861_adapter/hals/    |                                                 |
| iot_hardware/wifiiot_lite/hal_iot_gpio.c:1 | 用 IoTGpioInit / IoTGpioSetDir / IoTGpioSetOutputVal 这一层，最适合新工程 |                                                              |                                                 |
| GPIO，底层 SDK 写法                        | src/device/hisilicon/hispark_pegasus/sdk_liteos/include/hi_gpio.h:1 + src/device/hisilicon/hispark_pegasus/sdk_liteos/include/ |                                                              |                                                 |
| hi_io.h:1                                  | 底层实现主要在 SDK/boot 层，应用层通常不直接改               | 用 hi_gpio_* 和 hi_io_set_func，适合需要精细控制引脚复用的场景 |                                                 |
| 板级封装示例                               | src/vendor/pzkj/pz_hi3861/common/bsp/include/*.h             | src/vendor/pzkj/pz_hi3861/common/bsp/src/*.c                 | 比如 bsp_led.h/.c、bsp_key.h/.c、               |
| bsp_uart.h/.c                              |                                                              |                                                              |                                                 |
| Wi-Fi/BT 共存                              | src/device/hisilicon/hispark_pegasus/sdk_liteos/include/hi_btcoex_api.h:1 | 这份树里没有找到公开的 BT 业务栈 .c                          | 只有                                            |
| hi_wifi_btcoex_enable() 这种共存控制接口   |                                                              |                                                              |                                                 |
| 真实蓝牙业务栈                             | 本工程树里未找到                                             | 未找到                                                       | 如果你要 BLE/GATT/广播/扫描，这份源码树本身不够 |

  hi_io.h 里除了普通 GPIO 复用，还能看到 BT_FREQ、BT_STATUS、COEX_SWITCH、BT_ACTIVE、WLAN_ACTIVE 这类管脚功能，它们是共存信号，不是蓝牙应用层 API。

  2. 你新建工程时，最该改哪些文件

  如果你只是做一个新示例，最少改这几个：

  - 你的业务代码目录，比如 [src/vendor/pzkj/pz_hi3861/demo/42_ps2/template.c](D:
    \hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\vendor\pzkj\pz_hi3861\demo\42_ps2\template.c:64) 这种文件，写你自己的逻辑。
  - 你的示例构建文件，比如 [src/vendor/pzkj/pz_hi3861/demo/42_ps2/BUILD.gn](D:
    \hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\vendor\pzkj\pz_hi3861\demo\42_ps2\BUILD.gn:14)，这里决定要编哪些 .c、加哪些 include_dirs。
  - 顶层 demo 选择文件 [src/vendor/pzkj/pz_hi3861/demo/BUILD.gn](D:
    \hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\vendor\pzkj\pz_hi3861\demo\BUILD.gn:16)，这里决定当前编译哪一个 demo。
  - 工程配置 [.deveco/deveco.ini](D:\hi3861_hdu_iot_application\hi3861_hdu_iot_application\.deveco\deveco.ini:15)，这里决定板子、产品、工具链路径、串口、调
    试口。

  如果你是“完全新工程”但还是同一块 Hi3861 板子，我建议直接复制现有 42_ps2 目录，然后改成你自己的目录名和 BUILD.gn，比从零搭更稳。

  3. 编译路径和编译方式，是哪些文件决定的

  编译链路实际上是这样串起来的：

  - [.deveco/deveco.ini](D:\hi3861_hdu_iot_application\hi3861_hdu_iot_application\.deveco\deveco.ini:15) 决定环境是 hi3861、产品是
    wifiiot_hispark_pegasus@hisilicon、工具链路径、上传串口。
  - [src/applications/sample/wifi-iot/app/BUILD.gn](D:\hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\applications\sample\wifi-
    iot\app\BUILD.gn:16) 把系统应用入口指到 //vendor/pzkj/pz_hi3861/demo:demo。
  - [src/vendor/pzkj/pz_hi3861/demo/BUILD.gn](D:\hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\vendor\pzkj\pz_hi3861\demo\BUILD.gn:16) 决定当前
    demo 里启用哪个具体示例，比如现在是 42_ps2:template。
  - [src/vendor/pzkj/pz_hi3861/demo/42_ps2/BUILD.gn](D:
    \hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\vendor\pzkj\pz_hi3861\demo\42_ps2\BUILD.gn:14) 决定你的 .c 和头文件搜索路径。
  - [src/device/hisilicon/hispark_pegasus/sdk_liteos/BUILD.gn](D:
    \hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\device\hisilicon\hispark_pegasus\sdk_liteos\BUILD.gn:10) 和 [src/device/hisilicon/
    hispark_pegasus/sdk_liteos/config.gni](D:
    \hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\device\hisilicon\hispark_pegasus\sdk_liteos\config.gni:4) 决定板级工具链、编译参数、头文件路
    径和最终输出的 out 目录。
  - [src/base/iot_hardware/peripheral/BUILD.gn](D:\hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\base\iot_hardware\peripheral\BUILD.gn:18) 和
    [src/device/hisilicon/hispark_pegasus/hi3861_adapter/hals/iot_hardware/wifiiot_lite/BUILD.gn](D:
    \hi3861_hdu_iot_application\hi3861_hdu_iot_application\src\device\hisilicon\hispark_pegasus\hi3861_adapter\hals\iot_hardware\wifiiot_lite\BUILD.gn:14)
    决定 IoTGpio* 这一层是怎么落到芯片实现上的。

  这也是为什么这份工程最后的编译输出会落到 src/out/hispark_pegasus/wifiiot_hispark_pegasus，它本质上是由 board + product 组合推出来的。

  4. 你应该怎么改，才是正确做法

  - 如果你写普通 GPIO 业务，优先用 IoTGpio*，头文件是 iot_gpio.h。
  - 如果你需要改引脚复用，必须再加 hi_io.h，然后调用 hi_io_set_func()。
  - 如果你用的是板级封装，比如 LED、按键、蜂鸣器、PWM，直接参考 src/vendor/pzkj/pz_hi3861/common/bsp/include 和 src/vendor/pzkj/pz_hi3861/common/bsp/src 里
    的做法，把对应 bsp_*.c 加进你的 BUILD.gn。
  - 如果你只是想换一个自己的 demo，改 demo/BUILD.gn 里的 features，把 42_ps2:template 换成你的目录名和静态库名。
  - 如果你要换串口、烧录口、工具链路径，只改 .deveco/deveco.ini。
  - 如果你想做真正的蓝牙业务，而不是 Wi-Fi/BT 共存控制，这份源码树不够，需要额外的 BLE/BT 栈支持；在当前树里我没有找到对应的公开头文件和 .c 实现。