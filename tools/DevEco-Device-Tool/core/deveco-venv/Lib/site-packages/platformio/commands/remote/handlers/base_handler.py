import platformio.rpc.server
import platformio.commands.remote.handlers.session


class BaseHandler(platformio.rpc.server.Handler):

    @property
    def session(self) -> platformio.commands.remote.handlers.session.Session:
        return self.get_handler('session')
