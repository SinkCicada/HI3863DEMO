const {
  dataApi: { DependencyApi },
  http
} = window.top.HPM_CLI_UI

const dependencyApi = new DependencyApi();

const pluginName = "@hpm/hpm_cli_dag";

const render = () => {
  return http.get(`/resource/${pluginName}/data.json`).then((result) => {
    return result;
  });
}