"use strict";

var path = require('path');

var os = require('os');

var SocketType = {
  CONNECTED: 'CONNECTED',
  START: 'START',
  DATA: 'DATA',
  DONE: 'DONE',
  STOP: 'STOP',
  CHECKED: 'CHECKED'
};
var TaskType = {
  cmd: 'cmd',
  hook: 'hook',
  script: 'script',
  plugin: 'plugin'
};
var fileMap = new Map([['dataUrl', 'subsystem.json'], ['manifestUrl', 'manifest.json'], ['logicUrl', 'logic.js'], ['templateUrl', 'template.zip']]);
var DEFAULT_HISTORY_LOG_DIR = path.join(os.homedir(), '.hpm', 'log', 'history');
module.exports = {
  SocketType: SocketType,
  TaskType: TaskType,
  fileMap: fileMap,
  DEFAULT_HISTORY_LOG_DIR: DEFAULT_HISTORY_LOG_DIR
};