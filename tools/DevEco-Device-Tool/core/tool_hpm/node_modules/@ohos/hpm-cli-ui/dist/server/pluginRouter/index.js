"use strict";

//  注册插件上的路由
var express = require('express');

var router = express.Router();

var _require = require('./register'),
    resolveRouterPlugins = _require.resolveRouterPlugins;

var ServerApi = require('./serverApi');

resolveRouterPlugins(new ServerApi(router));

module.exports = function () {
  resolveRouterPlugins(new ServerApi(router));
};